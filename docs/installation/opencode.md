# Install for OpenCode

```bash
npx qa-automation-pack --yes
# or:
npx qa-automation-pack install --agent opencode --yes
```

- Skills: `.agents/skills/`
- Slash wrappers: `.opencode/commands/<skill>.md` (generated, ≤15 lines)
