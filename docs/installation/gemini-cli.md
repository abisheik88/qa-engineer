# Install for Gemini CLI

```bash
npx qa-automation-pack --yes
# or:
npx qa-automation-pack install --agent gemini-cli --yes
```

- Skills: `.agents/skills/`
- Command wrappers: `.gemini/commands/<skill>.toml`
